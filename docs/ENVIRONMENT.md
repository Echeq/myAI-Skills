# Environment Configuration

> Generated by `@ai-env --init` on 2026-07-10.

## Overview

This project uses environment variables for configuration. The canonical list of required variables is in `.env.example` at the project root.

## Setup

1. Copy `.env.example` → `.env`
2. Fill in the values
3. Run `@ai-env --validate` to confirm everything matches

## Variables

No environment variables were detected in the codebase at generation time.

As the project grows, add new variables to `.env.example` with a comment explaining their purpose:

```bash
# Database connection string
DATABASE_URL=postgresql://user:pass@localhost:5432/db

# External API key
API_KEY=sk-your-key-here
```

## Rules

- **Never commit `.env`** — it is excluded by `.gitignore`
- **Always document new variables** in `.env.example` and this file
- **Run `@ai-env --validate`** after changing `.env` to catch drift

---

**[⬆ Back to Top](#)** | **[📂 Skill Index](/docs/README.md)**
