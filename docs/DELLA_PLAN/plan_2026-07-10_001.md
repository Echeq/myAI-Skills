# Plan: Skill Review + Memory Migration for Router & Orchestrator

**Generated:** 2026-07-10 | **Model:** deepseek-v4-flash | **Version:** 1.0
**Request:** Review all skills for improvements and plan migration of router/orchestrator state to `.agents/memory/`

## Objective

Audit all 10 skills for improvement opportunities and migrate ai-router/ai-orchestrator runtime state from `.agents/skills/<name>/assets/` to `.agents/memory/<name>/`, following the pattern DELLA already uses.

---

## Part 1: Skill-by-Skill Review

### 1. ai-audit — Mostly good

| Aspect | Finding |
|--------|---------|
| ✅ | Uses `.agents/memory/ai-audit/last-audit.json` for regression tracking — correct location |
| ❌ | Directory `.agents/memory/ai-audit/` does not exist yet (the skill references it but it was never created) |
| ❌ | `.gitignore` has `!.agents/memory/DELLA/assets/plan/.gitkeep` but no equivalent for ai-audit — the entire `.agents/memory/` is gitignored, so ai-audit can't commit its regression tracking |
| ⚠️ | No fallback if `.agents/memory/ai-audit/last-audit.json` is missing on first run — the skill says "First audit: Baseline established" but doesn't handle the file-not-found gracefully |
| ⚠️ | No `--seed` or `--reset` mode to clear regression data |

**Action:** Create `.agents/memory/ai-audit/` with a `.gitkeep` and add `!.agents/memory/ai-audit/.gitkeep` to `.gitignore`. Add a first-run check in SKILL.md.

---

### 2. ai-config — Minor issues

| Aspect | Finding |
|--------|---------|
| ✅ | Solid validation rules for frontmatter, triggers, gitignore |
| ❌ | Description says `opencode.jsonc` but the actual file in the repo is `opencode.json` (no `c`). Both are valid formats — should say `opencode.json/.jsonc` |
| ❌ | Gitignore check still references old `.agents/skills/ai-router/assets/plan/` path — needs updating after migration |
| ⚠️ | Only validates `.gitignore` for a hardcoded list of paths; doesn't discover new paths dynamically |

**Action:** Update description to `opencode.json/.jsonc`. Update gitignore check rules after migration.

---

### 3. ai-docs — Clean

| Aspect | Finding |
|--------|---------|
| ✅ | Well-structured, clear modes, manual block preservation |
| ✅ | Cross-link template, diagram conventions, audit scoring |
| ⚠️ | The `--log` sub-module writes to `docs/log/` — not a problem, but could also support `.agents/memory/` as an alternative |
| ℹ️ | No changes needed |

---

### 4. ai-env — Clean

| Aspect | Finding |
|--------|---------|
| ✅ | Clear lifecycle: scan → init → validate → audit |
| ✅ | Safety boundary: never reads `.env` values |
| ℹ️ | No changes needed |

---

### 5. ai-git — Clean

| Aspect | Finding |
|--------|---------|
| ✅ | Token-efficient sub-module pattern |
| ✅ | Each sub-module is self-contained |
| ℹ️ | No changes needed |

---

### 6. ai-init — Good, one issue

| Aspect | Finding |
|--------|---------|
| ✅ | Comprehensive bootstrap flow, good phase design |
| ❌ | Session resume saves to `.agents/skills/ai-init/assets/session.json` — this is state data inside the skill definition directory. Should be `.agents/memory/ai-init/session.json` |
| ℹ️ | Well-documented, handles edge cases (greenfield, conflicting flags, existing output directory) |

**Action:** Move session persistence path to `.agents/memory/ai-init/session.json`.

---

### 7. ai-router — Needs migration

| Aspect | Finding |
|--------|---------|
| ❌ | State lives in `.agents/skills/ai-router/assets/state/` (current_plan.md, history.md) — should be `.agents/memory/ai-router/assets/state/` |
| ❌ | Plans saved to `.agents/skills/ai-router/assets/plan/` — should be `.agents/memory/ai-router/assets/plan/` |
| ❌ | All references in SKILL.md, references/worker.md, references/supervisor.md, references/manager.md use `assets/state/` and `assets/plan/` — all must change |
| ✅ | Pipeline design is solid (quick/plan/debug) |
| ⚠️ | The `--validate-opencode` check in config references `opencode.jsonc` but the actual file is `opencode.json` |

**Action:** Full path migration (detailed in Part 2).

---

### 8. ai-orchestrator — Needs migration

| Aspect | Finding |
|--------|---------|
| ❌ | `dag.py` hardcodes `STATE_FILE = ".agents/skills/ai-orchestrator/assets/state/task_states.json"` — must change |
| ❌ | State/plans in `.agents/skills/ai-orchestrator/assets/state/` and `assets/plan/` — should be `.agents/memory/ai-orchestrator/` |
| ❌ | References in SKILL.md, references/config.md, references/dag_engine.md, references/manager.md, references/worker.md, references/supervisor.md all use old paths |
| ✅ | Has `AI_ORCHESTRATOR_STATE_FILE` env var override (good forward-thinking) |
| ℹ️ | Dynamic classification, capability registry, 8-state FSM are well-designed |

**Action:** Full path migration (detailed in Part 2), including dag.py constant.

---

### 9. auto-report — Minor

| Aspect | Finding |
|--------|---------|
| ⚠️ | Saves partial session to `.config.md` in its own directory — should be `.agents/memory/auto-report/session.json` for consistency |
| ✅ | Output goes to `docs/auto-report/` which is gitignored — fine |
| ✅ | Good format fallback chain |

**Action:** Move session state to `.agents/memory/auto-report/`.

---

### 10. skill-search — Clean

| Aspect | Finding |
|--------|---------|
| ✅ | Clear GitHub API usage, good rate-limit handling |
| ✅ | Handles multi-file skills correctly |
| ℹ️ | No changes needed |

---

## Part 2: Memory Migration Plan — Router & Orchestrator

### Current state layout

```
.agents/skills/ai-router/
  assets/
    plan/            ← runtime artifacts (gitignored individually)
    state/           ← runtime state (gitignored individually)
  references/
  SKILL.md
  install.ps1
  install.sh

.agents/skills/ai-orchestrator/
  assets/
    plan/            ← runtime artifacts (gitignored individually)
    state/           ← runtime state (gitignored individually)
  dag.py
  references/
  SKILL.md
  install.ps1
  install.sh
```

### Target layout

```
.agents/memory/
  DELLA/
    assets/
      plan/
      state/
        current_plan.md
        history.md
  ai-router/                              ← NEW
    assets/
      plan/                                ← migrated from .agents/skills/ai-router/assets/plan/
      state/                               ← migrated from .agents/skills/ai-router/assets/state/
        current_plan.md
        history.md
  ai-orchestrator/                         ← NEW
    assets/
      plan/                                ← migrated from .agents/skills/ai-orchestrator/assets/plan/
      state/                               ← migrated from .agents/skills/ai-orchestrator/assets/state/
        task_states.json
        current_plan.md
        history.md
  ai-audit/                                ← CREATE (referenced but missing)
    .gitkeep
  ai-init/                                 ← CREATE (for session.json)
    .gitkeep
  auto-report/                             ← CREATE (for session state)
    .gitkeep
```

### Files to change

#### ai-router (6 files)

| # | File | Change |
|---|------|--------|
| 1 | `.agents/skills/ai-router/SKILL.md` | Replace all `assets/state/` → `.agents/memory/ai-router/assets/state/` and `assets/plan/` → `.agents/memory/ai-router/assets/plan/` (lines 55, 60-61, 119-121, 129) |
| 2 | `.agents/skills/ai-router/references/worker.md` | Update path references in line 7 |
| 3 | `.agents/skills/ai-router/references/supervisor.md` | Update plan file path in line 13 |
| 4 | `.agents/skills/ai-router/references/manager.md` | No path references — unchanged |
| 5 | `.agents/skills/ai-router/references/config.md` | No path references — unchanged |
| 6 | `.agents/skills/ai-router/references/init.md` | No path references — unchanged |

#### ai-orchestrator (8 files)

| # | File | Change |
|---|------|--------|
| 1 | `.agents/skills/ai-orchestrator/SKILL.md` | Replace all `assets/state/` and `assets/plan/` paths (lines 120, 135, 147, 164, 243-246) |
| 2 | `.agents/skills/ai-orchestrator/dag.py` | Change `STATE_FILE` constant (line 29) from `.agents/skills/ai-orchestrator/assets/state/task_states.json` to `.agents/memory/ai-orchestrator/assets/state/task_states.json` |
| 3 | `.agents/skills/ai-orchestrator/references/config.md` | Update `state_file:` and `plan_input:` paths (lines 88-89) |
| 4 | `.agents/skills/ai-orchestrator/references/dag_engine.md` | Update example command paths (line 10) |
| 5 | `.agents/skills/ai-orchestrator/references/worker.md` | Update path references (line 7) |
| 6 | `.agents/skills/ai-orchestrator/references/supervisor.md` | Update plan file path (line 22) |
| 7 | `.agents/skills/ai-orchestrator/references/manager.md` | No path references — unchanged |
| 8 | `.agents/skills/ai-orchestrator/references/init.md` | No path references — unchanged |

#### Shared infrastructure (4 files)

| # | File | Change |
|---|------|--------|
| 1 | `.gitignore` | Remove `.agents/skills/ai-router/assets/plan/Plan_*.md`, `assets/state/`, `.agents/skills/ai-orchestrator/assets/plan/Plan_*.md`, `plan_input.json`, `assets/state/`. Add `!.agents/memory/ai-router/.gitkeep` and `!.agents/memory/ai-orchestrator/.gitkeep` and similar for ai-audit/ai-init/auto-report. Or simpler: keep `!.agents/memory/*/assets/plan/.gitkeep` as a broad pattern |
| 2 | `.agents/skills/ai-config/SKILL.md` | Update gitignore validation check (line 44) to reference new paths |
| 3 | `AGENTS.md` | Update "Generated & gitignored paths" table |
| 4 | `docs/` | Auto-regenerated by `@ai-docs update` after SKILL.md changes |

#### Standalone agent files (actual findings)

| # | File | Status | Action |
|---|------|--------|--------|
| 1 | `agent/ROUTER.md` | ✅ Clean — no hardcoded paths (delegates to skill) | None |
| 2 | `agent/ORCHESTRATOR.md` | ❌ Line 135: `assets/state/task_states.json` | Update to `.agents/memory/ai-orchestrator/assets/state/task_states.json` (Phase D) |
| 3 | `agent/DELLA.md` | ✅ Already uses `.agents/memory/DELLA/` | None |
| 4 | `docs/agents/ROUTER.md` | ❌ Lines 15, 30: old `assets/state/` and `assets/plan/` paths | Auto-fixed by `@ai-docs update` (Phase G) |
| 5 | `docs/agents/ORCHESTRATOR.md` | ✅ Clean | None |
| 6 | `docs/agents/DELLA.md` | ✅ Clean | None |

### Risk assessment

| Risk | L | I | Mitigation |
|------|---|---|------------|
| dag.py hardcoded path breaks during migration | 2 | 5 | Update dag.py first (Phase C), test with `python dag.py status` |
| Existing state files lost | 2 | 3 | Copy from old to new in Phase E before removing old |
| References missed in doc files | 3 | 2 | `@ai-docs update` catches stale paths; audit after migration |
| Broken .gitignore leads to committed state | 1 | 4 | Already gitignored — `.agents/memory/` is a single gitignore entry |

### Gaps

- ai-audit, ai-init, and auto-report all have minor state persistence that should also move to `.agents/memory/`. This review flags them but Part 2 focuses on router + orchestrator as requested.

---

## Part 3: Additional Improvement Opportunities

### Low-effort, high-value

| # | Change | Skills affected | Effort |
|---|--------|-----------------|--------|
| 1 | Update `ai-config` description: `opencode.jsonc` → `opencode.json/.jsonc` | ai-config | 1 min |
| 2 | Create `.agents/memory/ai-audit/` with `.gitkeep` + update `.gitignore` | ai-audit | 2 min |
| 3 | Add graceful first-run handling for missing `last-audit.json` in ai-audit | ai-audit | 5 min |
| 4 | Move ai-init session path to `.agents/memory/ai-init/session.json` | ai-init | 5 min |
| 5 | Move auto-report partial session to `.agents/memory/auto-report/session.json` | auto-report | 5 min |

### Medium-effort

| # | Change | Skills/agents affected | Effort |
|---|--------|----------------------|--------|
| 6 | Full memory migration for ai-router (Part 2) | ai-router + infrastructure | 20 min |
| 7 | Full memory migration for ai-orchestrator (Part 2) | ai-orchestrator + infrastructure + dag.py | 25 min |
| 8 | Fix `agent/ORCHESTRATOR.md` stale path (line 135) | ORCHESTRATOR agent | 1 min |
| 9 | Regenerate all docs with `@ai-docs update` | All | 5 min |

### Strategic (future)

| # | Idea | Why |
|---|------|-----|
| A | Standardize `.agents/memory/<skill>/` as the universal pattern for ALL skills that need state | Single rule instead of per-skill exceptions |
| B | Add a `memory-path` frontmatter field to SKILL.md so skills declare their state location explicitly | Self-documenting, machine-readable |
| C | Add `ai-config --check` rule: warn if any skill stores state under `.agents/skills/` | Prevent regression |

---

## Part 4: Agent Review — `agent/ROUTER.md`, `agent/ORCHESTRATOR.md`, `agent/DELLA.md`

### 11. agent/ROUTER.md — Clean

| Aspect | Finding |
|--------|---------|
| ✅ | Thin wrapper that delegates to `skill("ai-router")` — no hardcoded state paths |
| ✅ | Good "model by complexity" cardinal rule |
| ✅ | PowerShell chaining noted (`if ($?) { }`) |
| ⚠️ | Line 92: references `references/config.md` — this is relative from within the skill, not the agent. The agent delegates, so this path is resolved inside the skill context. Correct as-is. |
| ℹ️ | No changes needed |

---

### 12. agent/ORCHESTRATOR.md — One stale path

| Aspect | Finding |
|--------|---------|
| ❌ | **Line 135:** `Check assets/state/task_states.json and dag.py status for the current graph.` — stale path. After migration should read: `Check .agents/memory/ai-orchestrator/assets/state/task_states.json` |
| ✅ | Good "complexity determines depth" rule, clear DAG flow documentation |
| ✅ | PowerShell chaining noted |
| ✅ | No other hardcoded `assets/` paths beyond line 135 |

**Action:** Update line 135 to reference `.agents/memory/ai-orchestrator/assets/state/task_states.json`.

---

### 13. agent/DELLA.md — Clean (reference implementation)

| Aspect | Finding |
|--------|---------|
| ✅ | Already uses `.agents/memory/DELLA/` correctly — this is the pattern router/orchestrator should follow |
| ✅ | Lines 44, 199, 207: all reference `.agents/memory/DELLA/assets/state/` — correct |
| ✅ | Clear phases (Check Memory → Read the Room → Think → Analyze → Write → Review → Present → Log) |
| ✅ | Self-review tiers, edge case handling, template |
| ⚠️ | Line 68: `glob agent/*.md` — this glob pattern works on both Windows and Unix |
| ℹ️ | No changes needed |

---

### 14. docs/agents/ (auto-generated duplicates)

| # | File | Finding |
|---|------|---------|
| 1 | `docs/agents/ROUTER.md` | ❌ Lines 15, 30: references old `assets/state/history.md`, `assets/plan/`, `assets/state/current_plan.md` — stale. These docs are auto-generated copies that must be regenerated after the migration |
| 2 | `docs/agents/ORCHESTRATOR.md` | ✅ No stale state path references |
| 3 | `docs/agents/DELLA.md` | ✅ Already uses `.agents/memory/DELLA/` — clean |

**Action:** `docs/agents/ROUTER.md` will be auto-fixed by `@ai-docs update` in Phase F.

---

## Part 5: Consolidated Migration Order

```
Phase A — Create directories + .gitkeep files
Phase B — Update ai-router SKILL.md + references
Phase C — Update ai-orchestrator SKILL.md + references + dag.py
Phase D — Update agent/ORCHESTRATOR.md (line 135 path)
Phase E — Update shared infrastructure (.gitignore, ai-config, AGENTS.md)
Phase F — Migrate any existing state files (manual copy)
Phase G — Regenerate docs with @ai-docs update → run @ai-config --check
```

### Phase details

| Phase | Files to change | Verification |
|-------|----------------|-------------|
| **A** | Create `.agents/memory/{ai-router,ai-orchestrator,ai-audit,ai-init,auto-report}/` dirs with `.gitkeep`; update `.gitignore` | `git status` shows `.gitkeep` files |
| **B** | `.agents/skills/ai-router/SKILL.md` (6 refs), `references/worker.md`, `references/supervisor.md` | `grep -n "assets/state\|assets/plan"` yields 0 results |
| **C** | `.agents/skills/ai-orchestrator/SKILL.md` (10 refs), `dag.py` line 29, `references/config.md`, `references/dag_engine.md`, `references/worker.md`, `references/supervisor.md` | `python dag.py status` works; `grep` yields 0 old refs |
| **D** | `agent/ORCHESTRATOR.md` line 135 | Manual review of line 135 |
| **E** | `.gitignore` (clean up per-skill entries), `.agents/skills/ai-config/SKILL.md` (gitignore check), `AGENTS.md` (paths table) | `@ai-config --check` passes |
| **F** | Copy any existing `.agents/skills/*/assets/state/*` and `plan/*` to `.agents/memory/*/` | `ls .agents/memory/*/assets/` shows expected files |
| **G** | Run `@ai-docs update` (regenerates `docs/` stale paths) | `@ai-config --check` passes |

---

## Handoff

Recommended: **Manual** (multi-phase, careful ordering required)

1. **Phase A:** Create directories + `.gitkeep` + update `.gitignore` → verify with `git status`
2. **Phase B:** Update ai-router SKILL.md + references → grep-verify no stale paths remain
3. **Phase C:** Update ai-orchestrator SKILL.md + dag.py + references → test `python dag.py status`
4. **Phase D:** Fix `agent/ORCHESTRATOR.md` line 135 path reference
5. **Phase E:** Update shared infrastructure (.gitignore, ai-config, AGENTS.md)
6. **Phase F:** Copy any existing state files (if they contain useful session data)
7. **Phase G:** Run `@ai-docs update` to regenerate docs → run `@ai-config --check` to validate
